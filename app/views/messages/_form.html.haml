.new_message-form
  = simple_form_for message, url: messages_path, html: {role: 'message-form'} do |f|
    = f.input :user_id, as: :hidden
    = f.input :online_id, as: :hidden
    .form-group
      = f.input_field :body, role: 'message-body', class: 'form-control', placeholder: 'Новое сообщение'
      %p{role: 'message-body-errors'}
  %p
    Загрузить фото
    #photos
      = simple_form_for Photo.new, url: photos_path do |photo|
        = photo.input_field :message_id, as: :hidden, value: message.id
        = photo.input_field :event_id, as: :hidden, value: message.online.event_id
        = photo.input_field :online_id, as: :hidden, value: message.online_id
        = photo.file_field :file, multiple: true, name: "photo[file]"
  %a.btn.btn-primary.pull-right{role: "submit-message"} Отправить




