.row.event-block
  .col-xs-3.user-info-block.event-user-block
    = image_tag @user.avatar.url(:thumb), class: 'round-avatar'
    = link_to "Редактировать профиль", my_edit_profile_path
    %h5= @user.full_name
    %small.user-desc= @user.bio
  .col-xs-9.last-messages-block
    - @user.onlines.each do |online|
      .plain-online-block
        %h4= link_to online.title, app_online_path(online)
        %p.small.online-desc= online.description
        %h6.last-messages-header Последине сообщения:
        - online.messages.last(3).each do |message|
          = render "messages/single_message", message: message